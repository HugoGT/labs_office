VITE_E2E_HOOK=1
VITE_COLYSEUS_URL=ws://localhost:2599

# Vacias A PROPOSITO, no por olvido: `loadEnv` tambien carga el `.env` local del
# desarrollador, asi que sin estas dos lineas un `.env` con credenciales reales
# se colaria en el bundle de e2e y la suite se toparia con la pantalla de login.
# Vacia = autenticacion desactivada (src/auth/authConfig.ts).
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_PROJECT_ID=
